# Discovery Toolkit — Trimmed for Integration

These files are from a research session. They are SOURCE MATERIAL, not a standalone pipeline.

- `map_investor_owned.py` — Adapt this logic. Read from existing DB, not CSV.
- `mine_ccr_links.py` — Build manual-input version only. Do not automate APEX form.
- `research_report_water_utility_directories.md` — Copy to docs/ as reference.
- `source_catalog_directories.yaml` — Merge entries into existing source_catalog table.
- `strategy_search_queries.yaml` — Review for novel query templates to merge into DiscoveryAgent.

DO NOT build new SDWIS download, search query generation, or YAML builder scripts.
These capabilities already exist in the repo.
